<h1>
  <%= "New Summary for \"#{@post.title}\"" %>
</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for summaries:</p>
     <ul>
       <li>Keep it simple, stupid.</li>
       <li>Never insult cats.</li>
       <li>Frown when you type.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for [@topic, @post, @summary] do |f| %>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_field :body, class: 'form-control', placeholder: "Enter summary text." %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>